#ifndef QUESTION_2_H
#define QUESTION_2_H

#include <vector>
using namespace std;

// Function to check if a number is prime
bool is_prime(int num);

// Function to return all prime numbers up to num
vector<int> get_primes(int num);

#endif
